overwrite: true
schema: "https://localhost:4000"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript-common"
      - "typescript-server"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
